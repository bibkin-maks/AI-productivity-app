# Core API
fastapi
uvicorn[standard]
python-multipart

# Environment
python-dotenv

# Validation
pydantic

# AI: chat completions + embeddings (vectors ranked with numpy)
openai
numpy

# PDF loading and chunking
pypdf
langchain-text-splitters

# Database
motor
pymongo

# Auth & security
python-jose[cryptography]
google-auth
requests

# Rate limiting
slowapi

# Optional: local neural voice for Purr Assist (github.com/rhasspy/piper).
# Install it and download a voice model (see backend/voices/README.md) to use it;
# without it /speak returns 503 and the app falls back to the browser's voice.
# piper-tts
